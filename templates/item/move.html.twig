<div class="modal-body">
    <h1>Move Item</h1>

    {{ form_start(form) }}
    {{ form_row(form.section) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

</div>