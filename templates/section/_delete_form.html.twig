<form method="post" action="{{ path('app_section_delete', {'id': section.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ section.id) }}">
    <button class="btn btn-danger w-100">Delete</button>
</form>
